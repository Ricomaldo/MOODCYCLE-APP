TESTS À VALIDER :
1. NavigationStore persiste filtres entre écrans
2. ChatView stable 30min (memory)
3. CalendarView <5 renders/changement mois
4. Phase API dynamique (pas "menstrual" hardcodé)

PROTOCOLE :
- Test 1 : Carnet → filtrer "Lutéale" → Chat → Carnet (filtre conservé?)
- Test 2 : DevTools Memory → Chat 30min → Delta <5MB
- Test 3 : Profiler → Calendar → 5 changements mois → <25 renders total
- Test 4 : Chat message → logs API → phase = phase actuelle